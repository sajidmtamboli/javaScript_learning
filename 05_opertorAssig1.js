var wordLengthSqaure = function (word) {
    var string = "JavaScript"
    console.log(`word is:- ${string}`);
    var length = string.length
    console.log(`The length of word is:- ${length}`);
    return length * length

}
var result = wordLengthSqaure("javaScript");
console.log(`Total word length square is:-${result}`);

console.log("======================================");

var wordLengthSqaure = function (word) {
    var string = "Angular"
    console.log(`word is:- ${string}`);
    var length = string.length
    console.log(`The length of word is:- ${length}`);
    return length * length

}
var output = wordLengthSqaure("Angular");
console.log(`Total word length square is:-${output}`);
console.log("======================================");

var wordLengthSqaure = function (word) {
    var string = "Developer"
    console.log(`word is:- ${string}`);
    var length = string.length
    console.log(`The length of word is:- ${length}`);
    return length * length
}
var answer=wordLengthSqaure("Developer");
console.log(`Total word length square is:- ${answer}`);
console.log("======================================");

var statment=function(){
    var string="I am Angular developer"
    console.log(`The given string is:- ${string}`);
    var length=string.length
    console.log(`The string length is:- ${length}`);
    console.log(`The reverse of string is:- ${string.split(" ").reverse()}`);
    var splitResult=string.split(" ");
    console.log(`string:- ${splitResult}`);
    console.log(`Total String words is:- ${splitResult.length}`);
    var leng1=22;
    var leng2=4;
    var divide=leng1/leng2;
    console.log(`String length divided by total no of words:- ${divide}`);
    console.log(`String length multiply by total no of word:- ${leng1*leng2}`);

}
statment()
    

