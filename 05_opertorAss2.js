var greterNumber = function(num1,num2){
var result= num1 > num2 ? num1: num2;
console.log(`Greter Number is:- ${result}`);

}
greterNumber(10,-10)

var greterNumber = function(num1,num2){
    var result= num1 > num2 ? num1: num2;
    console.log(`Greter Number is:- ${result}`);
    
    }
    greterNumber(800,899)
    console.log("========================================");

    var evenOrodd= function(num){
        var result= num%2==0 ? "even" : "odd";
        console.log(`The given no 29 is:-${result}`);
        
    }
    evenOrodd(29)

    var evenOrodd= function(num){
        var result= num%2==0 ? "even" : "odd";
        console.log(`The given no 44 is:-${result}`);
        
    }
    evenOrodd(44)

    var evenOrodd= function(num){
        var result= num%2==0 ? "even" : "odd";
        console.log(`The given no 0 is:-${result}`);
        
    }
    evenOrodd(0)

    var evenOrodd= function(num){
        var result= num%2==0 ? "even" : "odd";
        console.log(`The given no 101 is:-${result}`);
        
    }
    evenOrodd(101)
    console.log("========================================");

    var evenOrodd= function(num){
        var result= num%2==0 ? true : false;
        console.log(`The given no 29 is:-${result}`);
        
    }
    evenOrodd(29)

    var evenOrodd= function(num){
        var result= num%2==0 ? true : false;
        console.log(`The given no 44 is:-${result}`);
        
    }
    evenOrodd(44)

    var evenOrodd= function(num){
        var result= num%2==0 ? true : false;
        console.log(`The given no 0 is:-${result}`);
        
    }
    evenOrodd(0)

    var evenOrodd= function(num){
        var result= num%2==0 ? true : false;
        console.log(`The given no 101 is:-${result}`);
        
    }
    evenOrodd(101)

    console.log("========================================");

    var evenOrodd= function() {
    var string="JavaScript"
    console.log(`${string}`);
    var length=string.length
    console.log(`Length of string=${length}`);
    var result= length%2==0? "Even" : "Odd"
    console.log(`given word is odd or even= ${result}`);
    }
    evenOrodd()
    console.log("---------------------------------------");
    var evenOrodd= function() {
        var string="Developer"
        console.log(`${string}`);
        var length=string.length
        console.log(`Length of string=${length}`);
        var result= length%2==0? "Even" : "Odd"
        console.log(`given word is odd or even= ${result}`);
        }
        evenOrodd()
        console.log("---------------------------------------");
        var evenOrodd= function() {
            var string="Goggle"
            console.log(`${string}`);
            var length=string.length
            console.log(`Length of string=${length}`);
            var result= length%2==0? "Even" : "Odd"
            console.log(`given word is odd or even= ${result}`);
            }
            evenOrodd()
